#!/usr/bin/env bash

set-monitors

if [ -n "$1" ]; then
    case $1 in
        "cwm"|"i3"|"bspwm")
            launch-polybar.sh
            break
            ;;
    esac
fi
setbg.sh "$HOME/.local/share/background" || \
    setbg.sh "$HOME/.dotfiles/.local/share/dark-sunset.jpg"
