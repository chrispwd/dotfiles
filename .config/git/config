[alias]
  st=status
  cm="commit -m"
  co=checkout
  ll="log --oneline"
  lg="log --oneline --graph"
  pom="push origin master"
  po="push origin"
  wt="worktree"
  br="branch"
  pr="pull --rebase"
  A="add -A"
