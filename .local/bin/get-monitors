#!/bin/sh

outputs=$(xrandr --query | grep " connected" | cut -d" " -f1)

for outp in $outputs ; do
    case $outp in
        # meep
        *LVDS*)
            laptop_output=$outp
            ;;
        # stinky
        *eDP*)
            laptop_output=$outp
            ;;
        *HDMI*)
            external_monitor=$outp
            ;;
    esac
done

if [ -n "$external_monitor" ]; then
    case $laptop_output in
        *LVDS*)
            xrandr --output $laptop_output --auto --pos 400x1200 $external_monitor --output $external_monitor --mode 1920x1200 --pos 0x0 --primary
            ;;
        *eDP*)
            xrandr --output $laptop_output --auto --pos 0x600 $external_monitor --output $external_monitor --mode 1920x1200 --pos 1920x0 --primary
            ;;
    esac
else
    xrandr --output $laptop_output --auto
fi
