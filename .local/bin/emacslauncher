#!/bin/sh

if [ $(emacs --version | head -n1 | cut -d" " -f3 | cut -d'.' -f1) -lt 29 ]; then
    emacs --daemon --with-profile "mime"
else
    emacs --daemon --init-directory "$HOME/.config/mimemacs"
fi
